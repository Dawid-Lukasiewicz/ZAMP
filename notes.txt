1. Zwalnianie biblioteki z poprzedniej klasy jeśli została już otwarta
2. Libnterface powinien zawierać kolekcje wtyczek. Zastosowanie map

Na zajęciach początku etapu 2
ExecCmd(Scene *pScene, sdt::pair<std::mutex, int socket> *pCommChannel)

Poprawić Handlera, nie powinno być tak, że najpierw zczytuję polecenie 
i przeszukuję mape wtyczek